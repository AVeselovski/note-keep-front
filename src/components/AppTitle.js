import React from 'react';

const AppTitle = () => {
    return (
        <div className="app-title">
            <h1>ZERO TASKS</h1>
            <h2>
                Because there is probably going to be next to zero support for
                this app.
            </h2>
        </div>
    );
};

export default AppTitle;
